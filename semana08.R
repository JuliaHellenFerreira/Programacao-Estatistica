# Semana 08 - Algoritmos de ordenação

OrdenaRapido <- function(v){
  
  n <- length(v)
  
  if( n == 1)
    return(v)
  
# Busca do i
  i <- 2
  repeat{
    if( i < n){ #Não achei i, logo o pivo é o maior elemento. Trocar v[1] por v[n].
      
    }
  }
}